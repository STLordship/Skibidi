create database restorant;
USE restorant;
create table Soups (
   ID int NOT NULL,
   Soup_name varchar(255) NOT NULL,
   gramage int NOT NULL,
   price float NOT NULL
);

create table Appetizers (
    ID int NOT NULL,
    App_name varchar(255) NOT NULL,
    gramage int NOT NULL,
    price float NOT NULL
  );
  
create table Main_course (
	ID int NOT NULL,
    Course_name varchar(255) NOT NULL,
    gramage int NOT NULL,
    price float NOT NULL
);

Created the database with 3 tables for their respected categories- Soups, Appetizers, Main course.Excuse my broken english skills but I genuinely have no clue how to spell appetizers.
Hugs and kisses, your database "expert", Ivaylo :)
